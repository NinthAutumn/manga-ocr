export enum CategoryType {
  GENRE = "genre",
  TAG = "tag",
}

export enum ContentType {
  PARAGRAPH = "paragraph",
  IMAGE = "image",
}

export enum ChapterState {
  PUBLISHED = "published",
  DELETED = "deleted",
  DRAFT = "draft",
  SCHEDULED = "scheduled",
}
export enum FeatureType {
  PARAGRAPH = "paragraph",
  BLOCK = "block",
  WORD = "word",
}